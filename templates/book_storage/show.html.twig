{% extends 'base.html.twig' %}

{% block title %}BookStorage{% endblock %}

{% block body %}
    <h1>BookStorage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ book_storage.id }}</td>
            </tr>
            <tr>
                <th>Isbn</th>
                <td>{{ book_storage.isbn }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ book_storage.title }}</td>
            </tr>
            <tr>
                <th>Addedon</th>
                <td>{{ book_storage.addedon ? book_storage.addedon|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('book_storage_index') }}">back to list</a>

    <a href="{{ path('book_storage_edit', {'id': book_storage.id}) }}">edit</a>

    {{ include('book_storage/_delete_form.html.twig') }}
{% endblock %}